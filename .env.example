# auth & storage
DATABASE_URL=postgres://postgres:postgres@localhost:5432/langextract
REDIS_URL=redis://localhost:6379
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=documents
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin

# llm backends
GROQ_API_KEY=sk-...
OLLAMA_HOST=http://localhost:11434

# extraction defaults
DEFAULT_BACKEND=groq
DEFAULT_MODEL_GROQ=llama-3.1-70b-versatile
DEFAULT_MODEL_OLLAMA=llama3.1:8b-instruct
CONFIDENCE_THRESHOLD_CRITICAL=0.90

# OCR (dev + prod) — using tesseract.js
# OCR_LANG can be a space-separated list (e.g., "eng fra")
OCR_LANG=eng
# Optional path to local traineddata files to avoid network fetches
# Example: OCR_LANG_PATH=./.dev/tessdata
# If unset, tesseract.js may fetch from CDN on first run
# OCR_LANG_PATH=

# service ports
API_PORT=3001
WORKER_HTTP_PORT=3002
WEB_PORT=3000

# logging
# LOG_LEVEL one of: trace|debug|info|warn|error
LOG_LEVEL=info
# LOG_FORMAT: json|pretty
LOG_FORMAT=pretty
