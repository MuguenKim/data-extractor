{
  "name": "data-extractor-monorepo",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "echo Use package-level dev scripts",
    "dev:all": "node scripts/dev.cjs up",
    "stop:all": "node scripts/dev.cjs down",
    "restart:all": "node scripts/dev.cjs restart",
    "status:all": "node scripts/dev.cjs status",
    "compose:up": "docker compose -f infrastructure/docker-compose.yml up --build",
    "compose:down": "docker compose -f infrastructure/docker-compose.yml down -v",
    "lint": "echo 'lint placeholder'",
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
